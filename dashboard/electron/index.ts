// Scaffold marker for the electron/ module (Task 1 of the node-backend-port plan).
// TypeScript project references require at least one matching file to build;
// this placeholder satisfies that until Task 2 onward adds the real modules
// (schema.ts, config.ts, db.ts, watcher.ts, export.ts, server.ts, serve.ts,
// parsePair.ts). Safe to delete once real modules exist.
export {};
